version: '3'
services:
  animals:
    build: ./
    environment:
      - SPRING_PROFILES_ACTIVE=production
    ports:
     - "8080:8080"
     - "9080:9080"
    links:
      - mongo
  mongo:
    image: mongo
